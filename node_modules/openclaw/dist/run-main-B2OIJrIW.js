import { F as getPrimaryCommand, G as isValueToken, I as getVerboseFlag, L as hasFlag, M as getCommandPositionalsWithRootOptions, N as getFlagValue, P as getPositiveIntFlagValue, R as hasHelpOrVersion, j as getCommandPathWithRootOptions } from "./globals-d3aR1MYC.js";
import "./paths-BMo6kTge.js";
import { d as defaultRuntime, r as enableConsoleCapture } from "./subsystem-kl-vrkYi.js";
import "./boolean-DtWR5bt3.js";
import { a as normalizeEnv, c as parseCliProfileArgs, o as normalizeWindowsArgv, r as isTruthyEnvValue, s as applyCliProfileEnv } from "./entry.js";
import { Bn as loadDotEnv } from "./auth-profiles-CNyDTsy4.js";
import "./agent-scope-DuFk7JfV.js";
import "./utils-cwpAMi-t.js";
import "./openclaw-root-BFfBQ6FD.js";
import "./logger-BFQv53Hf.js";
import "./exec-t2VHjaVf.js";
import "./github-copilot-token-Byc_YVYE.js";
import "./host-env-security-lcjXF83D.js";
import { t as VERSION } from "./version-B7TNfFMY.js";
import "./env-vars-mSSOl7Rv.js";
import "./registry-ds-_TqV5.js";
import "./manifest-registry-DKS5Msti.js";
import "./dock-ChE50rbQ.js";
import "./model-bzHG4u0Y.js";
import "./pi-model-discovery-DTh-dRmO.js";
import "./frontmatter-BLUo-dxn.js";
import "./skills-B9NkSU2u.js";
import "./path-alias-guards-BLnvB3eQ.js";
import "./message-channel-vD1W0gaU.js";
import "./sessions-DNn6Jbbx.js";
import "./plugins-Co0ormRA.js";
import "./accounts-zRQn433-.js";
import "./accounts-K1IaOhUI.js";
import "./logging-CcxUDNcI.js";
import "./accounts-C35KnEXA.js";
import "./bindings-vxn_WYGq.js";
import "./send-M5GSjb2m.js";
import "./send-gAZjFaIg.js";
import "./subagent-registry-CkqrXKq4.js";
import "./paths-Bn3zjTqJ.js";
import "./chat-envelope-AUuZAcrC.js";
import "./client-CuIxivDk.js";
import "./call-DaJKh-6e.js";
import "./pairing-token-DfIpR3Pw.js";
import "./net-Bf8Z-b6p.js";
import "./tailnet-Dei3mqID.js";
import "./tokens-B3pi41Uf.js";
import "./with-timeout-DAUZJ1v0.js";
import "./deliver-DPAduhp1.js";
import "./diagnostic-Bq0LQSsy.js";
import "./diagnostic-session-state-B5Z0I32_.js";
import "./send-D1FslHaO.js";
import "./image-ops-B1XQ8UAg.js";
import "./pi-embedded-helpers-q7AlAFu4.js";
import "./sandbox-BTk3jOUP.js";
import "./tool-catalog-CDe8aNjS.js";
import "./chrome-Dt8bkIrk.js";
import "./tailscale-CcmGuDnz.js";
import "./auth-C24xIErm.js";
import "./server-context-CBtC0TLR.js";
import "./paths-8MkNWbbj.js";
import "./redact-kP6dI-RQ.js";
import { i as formatUncaughtError } from "./errors-DrflaMHL.js";
import "./fs-safe-kadrhuVr.js";
import "./proxy-env-BxxPVbHn.js";
import "./store-D0GaA-PU.js";
import "./ports-DuT3O3nk.js";
import "./trash-CzgjR7DR.js";
import "./server-middleware-BwkaybNn.js";
import "./tool-images-BCnln0pJ.js";
import "./thinking-BxCyPtl0.js";
import "./models-config-ERyMYjbg.js";
import "./exec-approvals-allowlist-CML34sxJ.js";
import "./exec-safe-bin-runtime-policy-rSDQoV6q.js";
import "./model-catalog-qZGHxvcI.js";
import "./fetch-C79EVYRM.js";
import { h as installUnhandledRejectionHandler } from "./audio-transcription-runner-Bq3uEE74.js";
import "./fetch-guard-BUZ81J83.js";
import "./image-BfgjMXod.js";
import "./tool-display-3t3R7qfs.js";
import "./api-key-rotation-Coo663aY.js";
import "./proxy-fetch-D397Vi7A.js";
import "./reply-prefix-D_u8yoXQ.js";
import "./chunk-DVb2QTEm.js";
import "./markdown-tables-B19v3Lps.js";
import "./ir-xeiny023.js";
import "./render-Bwu_WxwA.js";
import "./target-errors-fYvvIqQ5.js";
import "./commands-D38uo2qR.js";
import "./commands-registry-BO-Nhy_d.js";
import "./fetch-BAIuE19S.js";
import "./pairing-store-Cf2Lg6PN.js";
import "./exec-approvals-C9gzInje.js";
import "./nodes-screen-DZmNM68b.js";
import "./system-run-command-CgLq7n8_.js";
import "./session-utils-CJILahHK.js";
import "./session-cost-usage-BTlxpaWR.js";
import "./skill-commands-ux-0QZ8l.js";
import "./workspace-dirs-DdO8TO7t.js";
import "./channel-activity-BJktMV0Y.js";
import "./tables-CwnzpV_V.js";
import "./server-lifecycle-BfrWnmUb.js";
import "./stagger-Cek4Eizw.js";
import "./channel-selection-U4t706rb.js";
import "./plugin-auto-enable-DL7j3Ezh.js";
import "./send-B5QcnBt3.js";
import "./outbound-attachment-CRN3TdlB.js";
import "./delivery-queue-BiQk_YG-.js";
import "./send-BKZ2SX_s.js";
import "./resolve-route-T60oqPg3.js";
import "./pi-tools.policy-SQ828IQ3.js";
import "./proxy-CWm5cGVU.js";
import "./replies-Xw6QgU11.js";
import "./runtime-config-collectors-BJhmqEOW.js";
import "./command-secret-targets-B3asrqY0.js";
import "./onboard-helpers-CexGDVuD.js";
import "./prompt-style-CrypJNE0.js";
import "./pairing-labels-CYa_tb9P.js";
import "./session-meta-DnFhj0uA.js";
import "./memory-cli-Dlo3GrKB.js";
import "./manager-BpC_d51I.js";
import "./query-expansion-Dz8NAfL4.js";
import "./links-BMokj3K3.js";
import "./cli-utils-DjLJITj6.js";
import "./help-format-D_fwVCrM.js";
import "./progress-CcvPqJyX.js";
import { t as ensureOpenClawCliOnPath } from "./path-env-KRpaecCk.js";
import "./note-BxgfXB5v.js";
import "./issue-format-L1nDZPRU.js";
import { t as ensurePluginRegistryLoaded } from "./plugin-registry-D2ZZa9XN.js";
import { t as assertSupportedRuntime } from "./runtime-guard-DCaKUJXu.js";
import { t as emitCliBanner } from "./banner-BMfdXLml.js";
import "./doctor-config-flow-CEHVQZ3t.js";
import { n as ensureConfigReady } from "./config-guard-PiBmXyBy.js";
import process$1 from "node:process";
import { fileURLToPath } from "node:url";

//#region src/cli/program/routes.ts
const routeHealth = {
	match: (path) => path[0] === "health",
	loadPlugins: (argv) => !hasFlag(argv, "--json"),
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const verbose = getVerboseFlag(argv, { includeDebug: true });
		const timeoutMs = getPositiveIntFlagValue(argv, "--timeout");
		if (timeoutMs === null) return false;
		const { healthCommand } = await import("./health-GBxhlVbm.js").then((n) => n.i);
		await healthCommand({
			json,
			timeoutMs,
			verbose
		}, defaultRuntime);
		return true;
	}
};
const routeStatus = {
	match: (path) => path[0] === "status",
	loadPlugins: true,
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const deep = hasFlag(argv, "--deep");
		const all = hasFlag(argv, "--all");
		const usage = hasFlag(argv, "--usage");
		const verbose = getVerboseFlag(argv, { includeDebug: true });
		const timeoutMs = getPositiveIntFlagValue(argv, "--timeout");
		if (timeoutMs === null) return false;
		const { statusCommand } = await import("./status-DbyE3ZwA.js").then((n) => n.t);
		await statusCommand({
			json,
			deep,
			all,
			usage,
			timeoutMs,
			verbose
		}, defaultRuntime);
		return true;
	}
};
const routeSessions = {
	match: (path) => path[0] === "sessions" && !path[1],
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const allAgents = hasFlag(argv, "--all-agents");
		const agent = getFlagValue(argv, "--agent");
		if (agent === null) return false;
		const store = getFlagValue(argv, "--store");
		if (store === null) return false;
		const active = getFlagValue(argv, "--active");
		if (active === null) return false;
		const { sessionsCommand } = await import("./sessions-DsJp6v-C.js").then((n) => n.n);
		await sessionsCommand({
			json,
			store,
			agent,
			allAgents,
			active
		}, defaultRuntime);
		return true;
	}
};
const routeAgentsList = {
	match: (path) => path[0] === "agents" && path[1] === "list",
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const bindings = hasFlag(argv, "--bindings");
		const { agentsListCommand } = await import("./agents-JI6LnOFN.js").then((n) => n.t);
		await agentsListCommand({
			json,
			bindings
		}, defaultRuntime);
		return true;
	}
};
const routeMemoryStatus = {
	match: (path) => path[0] === "memory" && path[1] === "status",
	run: async (argv) => {
		const agent = getFlagValue(argv, "--agent");
		if (agent === null) return false;
		const json = hasFlag(argv, "--json");
		const deep = hasFlag(argv, "--deep");
		const index = hasFlag(argv, "--index");
		const verbose = hasFlag(argv, "--verbose");
		const { runMemoryStatus } = await import("./memory-cli-Dlo3GrKB.js").then((n) => n.t);
		await runMemoryStatus({
			agent,
			json,
			deep,
			index,
			verbose
		});
		return true;
	}
};
function getFlagValues(argv, name) {
	const values = [];
	const args = argv.slice(2);
	for (let i = 0; i < args.length; i += 1) {
		const arg = args[i];
		if (!arg || arg === "--") break;
		if (arg === name) {
			const next = args[i + 1];
			if (!isValueToken(next)) return null;
			values.push(next);
			i += 1;
			continue;
		}
		if (arg.startsWith(`${name}=`)) {
			const value = arg.slice(name.length + 1).trim();
			if (!value) return null;
			values.push(value);
		}
	}
	return values;
}
const routes = [
	routeHealth,
	routeStatus,
	routeSessions,
	routeAgentsList,
	routeMemoryStatus,
	{
		match: (path) => path[0] === "config" && path[1] === "get",
		run: async (argv) => {
			const positionals = getCommandPositionalsWithRootOptions(argv, {
				commandPath: ["config", "get"],
				booleanFlags: ["--json"]
			});
			if (!positionals || positionals.length !== 1) return false;
			const pathArg = positionals[0];
			if (!pathArg) return false;
			const json = hasFlag(argv, "--json");
			const { runConfigGet } = await import("./config-cli-1bYZp5n5.js");
			await runConfigGet({
				path: pathArg,
				json
			});
			return true;
		}
	},
	{
		match: (path) => path[0] === "config" && path[1] === "unset",
		run: async (argv) => {
			const positionals = getCommandPositionalsWithRootOptions(argv, { commandPath: ["config", "unset"] });
			if (!positionals || positionals.length !== 1) return false;
			const pathArg = positionals[0];
			if (!pathArg) return false;
			const { runConfigUnset } = await import("./config-cli-1bYZp5n5.js");
			await runConfigUnset({ path: pathArg });
			return true;
		}
	},
	{
		match: (path) => path[0] === "models" && path[1] === "list",
		run: async (argv) => {
			const provider = getFlagValue(argv, "--provider");
			if (provider === null) return false;
			const all = hasFlag(argv, "--all");
			const local = hasFlag(argv, "--local");
			const json = hasFlag(argv, "--json");
			const plain = hasFlag(argv, "--plain");
			const { modelsListCommand } = await import("./models-b8f546MT.js").then((n) => n.t);
			await modelsListCommand({
				all,
				local,
				provider,
				json,
				plain
			}, defaultRuntime);
			return true;
		}
	},
	{
		match: (path) => path[0] === "models" && path[1] === "status",
		run: async (argv) => {
			const probeProvider = getFlagValue(argv, "--probe-provider");
			if (probeProvider === null) return false;
			const probeTimeout = getFlagValue(argv, "--probe-timeout");
			if (probeTimeout === null) return false;
			const probeConcurrency = getFlagValue(argv, "--probe-concurrency");
			if (probeConcurrency === null) return false;
			const probeMaxTokens = getFlagValue(argv, "--probe-max-tokens");
			if (probeMaxTokens === null) return false;
			const agent = getFlagValue(argv, "--agent");
			if (agent === null) return false;
			const probeProfileValues = getFlagValues(argv, "--probe-profile");
			if (probeProfileValues === null) return false;
			const probeProfile = probeProfileValues.length === 0 ? void 0 : probeProfileValues.length === 1 ? probeProfileValues[0] : probeProfileValues;
			const json = hasFlag(argv, "--json");
			const plain = hasFlag(argv, "--plain");
			const check = hasFlag(argv, "--check");
			const probe = hasFlag(argv, "--probe");
			const { modelsStatusCommand } = await import("./models-b8f546MT.js").then((n) => n.t);
			await modelsStatusCommand({
				json,
				plain,
				check,
				probe,
				probeProvider,
				probeProfile,
				probeTimeout,
				probeConcurrency,
				probeMaxTokens,
				agent
			}, defaultRuntime);
			return true;
		}
	}
];
function findRoutedCommand(path) {
	for (const route of routes) if (route.match(path)) return route;
	return null;
}

//#endregion
//#region src/cli/route.ts
async function prepareRoutedCommand(params) {
	const suppressDoctorStdout = hasFlag(params.argv, "--json");
	emitCliBanner(VERSION, { argv: params.argv });
	await ensureConfigReady({
		runtime: defaultRuntime,
		commandPath: params.commandPath,
		...suppressDoctorStdout ? { suppressDoctorStdout: true } : {}
	});
	if (typeof params.loadPlugins === "function" ? params.loadPlugins(params.argv) : params.loadPlugins) ensurePluginRegistryLoaded();
}
async function tryRouteCli(argv) {
	if (isTruthyEnvValue(process.env.OPENCLAW_DISABLE_ROUTE_FIRST)) return false;
	if (hasHelpOrVersion(argv)) return false;
	const path = getCommandPathWithRootOptions(argv, 2);
	if (!path[0]) return false;
	const route = findRoutedCommand(path);
	if (!route) return false;
	await prepareRoutedCommand({
		argv,
		commandPath: path,
		loadPlugins: route.loadPlugins
	});
	return route.run(argv);
}

//#endregion
//#region src/cli/run-main.ts
function rewriteUpdateFlagArgv(argv) {
	const index = argv.indexOf("--update");
	if (index === -1) return argv;
	const next = [...argv];
	next.splice(index, 1, "update");
	return next;
}
function shouldSkipPluginCommandRegistration(params) {
	if (params.hasBuiltinPrimary) return true;
	if (!params.primary) return hasHelpOrVersion(params.argv);
	return false;
}
function shouldEnsureCliPath(argv) {
	if (hasHelpOrVersion(argv)) return false;
	const [primary, secondary] = getCommandPathWithRootOptions(argv, 2);
	if (!primary) return true;
	if (primary === "status" || primary === "health" || primary === "sessions") return false;
	if (primary === "config" && (secondary === "get" || secondary === "unset")) return false;
	if (primary === "models" && (secondary === "list" || secondary === "status")) return false;
	return true;
}
async function runCli(argv = process$1.argv) {
	let normalizedArgv = normalizeWindowsArgv(argv);
	const parsedProfile = parseCliProfileArgs(normalizedArgv);
	if (!parsedProfile.ok) throw new Error(parsedProfile.error);
	if (parsedProfile.profile) applyCliProfileEnv({ profile: parsedProfile.profile });
	normalizedArgv = parsedProfile.argv;
	loadDotEnv({ quiet: true });
	normalizeEnv();
	if (shouldEnsureCliPath(normalizedArgv)) ensureOpenClawCliOnPath();
	assertSupportedRuntime();
	if (await tryRouteCli(normalizedArgv)) return;
	enableConsoleCapture();
	const { buildProgram } = await import("./program-B9_KMUzE.js");
	const program = buildProgram();
	installUnhandledRejectionHandler();
	process$1.on("uncaughtException", (error) => {
		console.error("[openclaw] Uncaught exception:", formatUncaughtError(error));
		process$1.exit(1);
	});
	const parseArgv = rewriteUpdateFlagArgv(normalizedArgv);
	const primary = getPrimaryCommand(parseArgv);
	if (primary) {
		const { getProgramContext } = await import("./program-context-Bdw9YypP.js").then((n) => n.n);
		const ctx = getProgramContext(program);
		if (ctx) {
			const { registerCoreCliByName } = await import("./command-registry-BDeriI6F.js").then((n) => n.t);
			await registerCoreCliByName(program, ctx, primary, parseArgv);
		}
		const { registerSubCliByName } = await import("./register.subclis-GK5pfSQG.js").then((n) => n.a);
		await registerSubCliByName(program, primary);
	}
	if (!shouldSkipPluginCommandRegistration({
		argv: parseArgv,
		primary,
		hasBuiltinPrimary: primary !== null && program.commands.some((command) => command.name() === primary)
	})) {
		const { registerPluginCliCommands } = await import("./cli-DRqcgfoT.js");
		const { loadConfig } = await import("./auth-profiles-CNyDTsy4.js").then((n) => n.j);
		registerPluginCliCommands(program, loadConfig());
	}
	await program.parseAsync(parseArgv);
}

//#endregion
export { runCli };